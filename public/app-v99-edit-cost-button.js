(()=>{
 function run(){if(state?.view!=='daily-profits')return;document.querySelectorAll('.profit-order-card').forEach(card=>{card.querySelectorAll('button').forEach(b=>{if(b.textContent.trim()==='تعديل جمل الطلب')b.remove()});if(card.querySelector('.profit-edit-cost-btn'))return;const input=card.querySelector('.profit-cost');if(!input)return;const btn=document.createElement('button');btn.type='button';btn.className='btn btn-soft profit-edit-cost-btn';btn.textContent='تعديل كوست الطلب';btn.style.margin='8px 0';input.insertAdjacentElement('afterend',btn);btn.onclick=()=>{input.removeAttribute('readonly');input.removeAttribute('disabled');input.readOnly=false;input.disabled=false;input.style.pointerEvents='auto';card.dataset.manualCostOverride='1';card.dataset.manualCostEditing='1';card.dataset.offerPartialDone='1';card.dataset.singlePartialDone='1';input.focus();input.select();btn.textContent='عدّل الكوست ثم اضغط حفظ الحساب';};});}
 new MutationObserver(()=>setTimeout(run,30)).observe(document.documentElement,{childList:true,subtree:true});setInterval(run,400);run();
})();