(()=>{
 function unlock(input){input.removeAttribute('readonly');input.removeAttribute('disabled');input.readOnly=false;input.disabled=false;input.tabIndex=0;input.style.pointerEvents='auto';input.style.userSelect='text';input.style.webkitUserSelect='text';input.style.opacity='1';input.style.background='#fff'}
 function updateProfit(card){const amount=+(card.querySelector('.profit-amount')?.value||0),fee=+(card.querySelector('.profit-fee')?.value||0),cost=+(card.querySelector('.profit-cost')?.value||0),profit=amount-fee-cost;const profitBox=card.querySelector('.profit-net,.profit-value,.profit-total,[data-profit-value]');if(profitBox){profitBox.textContent=`${profit.toFixed(2)} د.أ`}const dark=[...card.querySelectorAll('div,strong,span')].find(x=>/صافي الربح/.test(x.previousElementSibling?.textContent||'')||(/د\.أ/.test(x.textContent||'')&&x.closest('.profit-order-card')===card&&x.className?.toString().includes('profit')));if(dark&&dark!==profitBox)dark.textContent=`${profit.toFixed(2)} د.أ`;return{amount,fee,cost,profit}}
 async function save(card){const id=+card.dataset.id;if(!id)return;const v=updateProfit(card);try{const d=await api('/orders/'+id),o=d.order;await api('/orders/'+id+'/outcome',{method:'PUT',body:JSON.stringify({delivery_status:o.delivery_status,printed:+(o.printed||0),delivered_amount:v.amount,delivery_fee:v.fee,cash_collected:Math.max(0,v.amount-v.fee),cost_of_goods:v.cost,delivered_pieces:+(o.delivered_pieces||0),returned_pieces:+(o.returned_pieces||0),settlement_note:o.settlement_note||''})});toast('تم حفظ كوست الطلب')}catch(e){toast(e.message||'تعذر حفظ الكوست')}}
 function run(){if(state?.view!=='daily-profits')return;document.querySelectorAll('.profit-order-card').forEach(card=>{const input=card.querySelector('.profit-cost');if(!input)return;unlock(input);if(input.dataset.manualCostBound==='1')return;input.dataset.manualCostBound='1';const manual=()=>{card.dataset.manualCostOverride='1';card.dataset.manualCostEditing='1';card.dataset.v92CostKey=`manual:${input.value}`;card.dataset.offerPartialDone='1';card.dataset.singlePartialDone='1';updateProfit(card)};input.addEventListener('pointerdown',()=>{unlock(input);card.dataset.manualCostEditing='1'});input.addEventListener('touchstart',()=>{unlock(input);card.dataset.manualCostEditing='1'},{passive:true});input.addEventListener('focus',()=>{unlock(input);card.dataset.manualCostEditing='1'});input.addEventListener('input',manual);input.addEventListener('change',manual);input.addEventListener('blur',()=>{setTimeout(()=>delete card.dataset.manualCostEditing,500)});const btn=[...card.querySelectorAll('button')].find(b=>b.textContent.trim()==='تعديل كوست الطلب');if(btn&&!btn.dataset.manualCostClick){btn.dataset.manualCostClick='1';btn.onclick=()=>{unlock(input);input.focus();input.select?.()}}const saveBtn=[...card.querySelectorAll('button')].find(b=>b.textContent.includes('حفظ الحساب'));if(saveBtn&&!saveBtn.dataset.manualCostSave){saveBtn.dataset.manualCostSave='1';saveBtn.addEventListener('click',()=>save(card),true)}})}
 new MutationObserver(()=>setTimeout(run,20)).observe(document.documentElement,{childList:true,subtree:true,attributes:true,attributeFilter:['readonly','disabled']});setInterval(run,250);run();
})();