(()=>{
 function unlock(input){input.removeAttribute('readonly');input.removeAttribute('disabled');input.readOnly=false;input.disabled=false;input.tabIndex=0;input.style.pointerEvents='auto';input.style.userSelect='text';input.style.webkitUserSelect='text';input.style.opacity='1';input.style.background='#fff'}
 function updateProfit(card){const amount=+(card.querySelector('.profit-amount')?.value||0),fee=+(card.querySelector('.profit-fee')?.value||0),cost=+(card.querySelector('.profit-cost')?.value||0),profit=amount-fee-cost;const profitBox=card.querySelector('.profit-net');if(profitBox)profitBox.textContent=money(profit);return{amount,fee,cost,profit}}
 async function save(card){const id=+card.dataset.id;if(!id)return;const v=updateProfit(card),partialItems=(card.querySelector('.profit-partial-items')?.value||'').trim();await api('/orders/'+id+'/profit-review',{method:'PUT',body:JSON.stringify({delivered_amount:v.amount,delivery_fee:v.fee,cost_of_goods:v.cost,partial_received_items:partialItems})});toast('تم حفظ كوست الطلب');await window.CORVEX_RELOAD_DAILY_PROFITS?.(window.CORVEX_ACTIVE_PROFIT_SETTLEMENT||card.dataset.settlementId||'')}
 function run(){if(state?.view!=='daily-profits')return;document.querySelectorAll('.profit-order-card').forEach(card=>{const input=card.querySelector('.profit-cost');if(!input)return;if(card.dataset.manualCost==='1')card.dataset.manualCostOverride='1';unlock(input);if(input.dataset.manualCostBound!=='1'){input.dataset.manualCostBound='1';const manual=e=>{if(!e.isTrusted)return;card.dataset.manualCostOverride='1';card.dataset.manualCostEditing='1';card.dataset.manualCostValue=String(input.value);card.dataset.v92CostKey=`manual:${input.value}`;card.dataset.offerPartialDone='1';card.dataset.singlePartialDone='1';updateProfit(card)};input.addEventListener('pointerdown',()=>{unlock(input);card.dataset.manualCostEditing='1'});input.addEventListener('touchstart',()=>{unlock(input);card.dataset.manualCostEditing='1'},{passive:true});input.addEventListener('focus',()=>{unlock(input);card.dataset.manualCostEditing='1'});input.addEventListener('input',manual);input.addEventListener('change',manual);input.addEventListener('blur',()=>{setTimeout(()=>delete card.dataset.manualCostEditing,500)})}
   const saveBtn=[...card.querySelectorAll('button')].find(b=>b.textContent.includes('حفظ الحساب'));if(saveBtn&&!saveBtn.dataset.manualCostSave){saveBtn.dataset.manualCostSave='1';saveBtn.addEventListener('click',async e=>{e.preventDefault();e.stopImmediatePropagation();saveBtn.disabled=true;try{await save(card)}catch(err){toast(err.message||'تعذر حفظ الكوست')}finally{saveBtn.disabled=false}},true)}
  })}
 document.addEventListener('corvex:profits-rendered',run);run();
})();
