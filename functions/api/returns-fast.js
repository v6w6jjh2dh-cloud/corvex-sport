const json=(data,status=200)=>new Response(JSON.stringify(data),{status,headers:{'content-type':'application/json; charset=utf-8'}});
async function body(req){try{return await req.json()}catch{return {}}}
async function auth(request,env){const h=request.headers.get('authorization')||'',token=h.startsWith('Bearer ')?h.slice(7):'';if(!token)return null;return await env.DB.prepare(`SELECT u.id,u.username,u.display_name,u.role,u.is_active FROM sessions s JOIN users u ON u.id=s.user_id WHERE s.token=? AND s.expires_at>datetime('now') AND u.is_active=1`).bind(token).first()}
function allowed(u){return !!u&&(u.role==='admin'||String(u.username||'').toLowerCase()==='shadi'||String(u.display_name||'').toLowerCase()==='shadi')}
function codeOf(v=''){const m=String(v||'').match(/(\d+)\s*$/);return m?Number(m[1]):0}
function clean(v=''){return String(v||'').replace(/\s+/g,' ').trim()}
const PRODUCT_RULES=[['بنطلون جيوب سحاب',/بنطلون\s*جيوب\s*سحاب/i],['بنطلون رياضة سحاب',/بنطلون\s*رياض[هة]?\s*سحاب/i],['رياضة سحاب',/(?:رياض[هة]\s*سحاب|سحاب\s*رياض[هة]|(?:^|\s)سحاب(?=\s|$))/i],['قطن سبور',/(?:قطن\s*سبور|رياض[هة]\s*قطن)/i],['كاردونيه',/(?:طقم\s*|بجام[هة]\s*)?كاردوني[هة]/i],['ريبوك',/ريبوك/i],['باريس',/باريس/i],['بنطلون تكيف',/(?:بنطلون\s*)?(?:تكيف|يكتف|تكف)/i],['بنطلون تركي',/بنطلون\s*تركي/i],['بنطلون زرار',/(?:بنطلون\s*)?زرار/i],['بنطلون جيوب',/(?:بنطلون\s*)?(?:جيوب|جيب\s*عادي)/i],['أولد مني',/(?:اولد|أولد)\s*مني/i],['بلوزة ليكرا',/(?:بلوز[هة]|بلوزه|تيشيرت)?\s*ليكرا/i],['بلوزة الحصان',/(?:بلوز[هة]|بلوزه|تيشيرت)?\s*(?:الحصان|هيرمز|اتش|\bh\b)/i],['بلوزة M',/(?:بلوز[هة]|بلوزه|تيشيرت)?\s*(?:حرف\s*)?(?:ام|m6?|M6?)(?=\s|\d|$)/i],['بولو تريكو تطريز',/(?:بلوز[هة]|بلوزه|تيشيرت)?\s*(?:(?:بولو\s*)?تريكو\s*تطريز|تطريز\s*تريكو(?:\s*بولو)?)/i],['بولو تريكو',/(?:بلوز[هة]|بلوزه|تيشيرت)?\s*(?:بولو\s*تريكو|تريكو\s*بولو)/i],['بولو صيفي',/(?:بلوز[هة]|بلوزه|تيشيرت)?\s*(?:بولو\s*صيفي|صيفي\s*بولو)/i],['سادة تطريز',/(?:بلوز[هة]|بلوزه|تيشيرت)?\s*(?:ساد[هة]\s*تطريز|تطريز\s*ساد[هة])/i],['سادة بولو',/(?:بلوز[هة]|بلوزه|تيشيرت)?\s*(?:ساد[هة]\s*بولو|بولو\s*ساد[هة])/i],['بلوزة سادة',/(?:بلوز[هة]|بلوزه|تيشيرت)\s*ساد[هة]/i],['بولو ترند',/بولو\s*ترند/i],['بجامة جاكار',/(?:ترين(?:غ|نغ)\s*)?جاكار|بجام[هة]\s*جاكار|جاكار\s*بجام[هة]/i],['ترينغ',/ترين(?:غ|نغ)/i],['بولو',/بولو/i]];
const COLOR_RULES=[['اسود',/(?:^|\s|و)(?:اسود|أسود|سود)(?=\s|$)/i],['بني',/(?:^|\s|و)بني(?=\s|$)/i],['بيج',/(?:^|\s|و)بيج(?=\s|$)/i],['ابيض',/(?:^|\s|و)(?:ابيض|أبيض)(?=\s|$)/i],['سكني',/(?:^|\s|و)سكني(?=\s|$)/i],['رصاصي',/(?:^|\s|و)رصاصي(?=\s|$)/i],['كحلي',/(?:^|\s|و)كحلي(?=\s|$)/i],['زيتي',/(?:^|\s|و)زيتي(?=\s|$)/i],['خمري',/(?:^|\s|و)خمري(?=\s|$)/i],['احمر',/(?:^|\s|و)(?:احمر|أحمر)(?=\s|$)/i],['ازرق',/(?:^|\s|و)(?:ازرق|أزرق)(?=\s|$)/i],['اخضر',/(?:^|\s|و)(?:اخضر|أخضر)(?=\s|$)/i],['رمادي',/(?:^|\s|و)رمادي(?=\s|$)/i],['سكري',/(?:^|\s|و)سكري(?=\s|$)/i],['نهدي',/(?:^|\s|و)نهدي(?=\s|$)/i],['موف',/(?:^|\s|و)موف(?=\s|$)/i]];
function arNum(v=''){return String(v).replace(/[٠١٢٣٤٥٦٧٨٩]/g,d=>String('٠١٢٣٤٥٦٧٨٩'.indexOf(d)))}
function qtyFromLine(line=''){const s=arNum(line);let m=s.match(/(?:عدد|العدد|كمية|كميه)\s*[:=]?\s*(\d+)/i);if(m)return Number(m[1]);m=s.match(/(?:^|\s)(\d+)\s*(?:قطعة|قطع|حبة|حبات)\b/i);if(m)return Number(m[1]);m=s.match(/[x×*]\s*(\d+)/i);if(m)return Number(m[1]);if(/(?:ثلاث[هة]?|3)\s*(?:ال)?وان|ثلاث\s*الالوان|ثلاثة\s*ألوان/i.test(s))return 3;if(/لونين|لونان|2\s*لون/i.test(s))return 2;if(/\bالعرض\b/i.test(s))return 3;let n=0;for(const [,re] of COLOR_RULES)if(re.test(s))n++;return n||1}
function itemsFromCurrentNotes(notes=''){const lines=String(notes||'').replace(/\r/g,'\n').split(/\n+/).map(x=>x.trim()).filter(Boolean),out=[];for(const line of lines){let best=null;for(const [name,re] of PRODUCT_RULES){const m=line.match(re);if(m&&(!best||m[0].length>best.len))best={name,len:m[0].length}}if(best)out.push({name:best.name,quantity:Math.max(1,qtyFromLine(line))})}if(!out.length){const whole=clean(notes);let best=null;for(const [name,re] of PRODUCT_RULES){const m=whole.match(re);if(m&&(!best||m[0].length>best.len))best={name,len:m[0].length}}if(best)out.push({name:best.name,quantity:Math.max(1,qtyFromLine(whole))})}const map=new Map();for(const x of out)map.set(x.name,(map.get(x.name)||0)+x.quantity);return [...map].map(([name,quantity])=>({name,quantity}))}
async function ensure(env){await env.DB.prepare(`CREATE TABLE IF NOT EXISTS return_events(id INTEGER PRIMARY KEY AUTOINCREMENT,order_id INTEGER NOT NULL UNIQUE,return_type TEXT NOT NULL DEFAULT 'full',reason TEXT NOT NULL DEFAULT '',notes TEXT NOT NULL DEFAULT '',created_by INTEGER NOT NULL,created_at TEXT NOT NULL DEFAULT (datetime('now')))`).run();await env.DB.prepare(`CREATE TABLE IF NOT EXISTS return_items(id INTEGER PRIMARY KEY AUTOINCREMENT,return_id INTEGER NOT NULL,item_name TEXT NOT NULL,quantity INTEGER NOT NULL DEFAULT 1,created_at TEXT NOT NULL DEFAULT (datetime('now')),UNIQUE(return_id,item_name))`).run()}
async function detail(env,orderCode){const row=await env.DB.prepare(`SELECT r.*,o.order_code,o.recipient_name,o.phone,o.order_notes,o.amount,o.area,o.detailed_address,s.name store_name,u.display_name created_by_name FROM return_events r JOIN orders o ON o.id=r.order_id LEFT JOIN stores s ON s.id=o.store_id LEFT JOIN users u ON u.id=r.created_by WHERE o.order_code=?`).bind(orderCode).first();if(!row)return null;row.items=(await env.DB.prepare('SELECT id,item_name,quantity FROM return_items WHERE return_id=? ORDER BY id').bind(row.id).all()).results||[];return row}
async function dashboard(env){const summary=await env.DB.prepare(`SELECT COUNT(*) total_returns,SUM(CASE WHEN return_type='full' THEN 1 ELSE 0 END) full_returns,SUM(CASE WHEN return_type='partial' THEN 1 ELSE 0 END) partial_returns FROM return_events`).first();const pieces=await env.DB.prepare('SELECT COALESCE(SUM(quantity),0) n FROM return_items').first();const top=(await env.DB.prepare(`SELECT item_name,SUM(quantity) returned_quantity,COUNT(DISTINCT return_id) return_orders FROM return_items GROUP BY item_name ORDER BY returned_quantity DESC,return_orders DESC,item_name LIMIT 30`).all()).results||[];const history=(await env.DB.prepare(`SELECT r.id,r.return_type,r.reason,r.notes,r.created_at,o.order_code,o.recipient_name,o.phone,o.amount,o.area,o.detailed_address,o.order_notes,s.name store_name,u.display_name created_by_name FROM return_events r JOIN orders o ON o.id=r.order_id LEFT JOIN stores s ON s.id=o.store_id LEFT JOIN users u ON u.id=r.created_by ORDER BY r.id DESC LIMIT 100`).all()).results||[];for(const r of history)r.items=(await env.DB.prepare('SELECT id,item_name,quantity FROM return_items WHERE return_id=? ORDER BY id').bind(r.id).all()).results||[];return {summary:{...summary,returned_pieces:Number(pieces?.n||0)},top_items:top,returns:history}}
export async function onRequest({request,env}){const u=await auth(request,env);if(!allowed(u))return json({error:'غير مصرح لمسح المرتجعات'},403);await ensure(env);const url=new URL(request.url),method=request.method.toUpperCase();if(method==='GET'){const oc=Number(url.searchParams.get('order_code')||0);if(oc){const d=await detail(env,oc);return d?json({return_event:d}):json({error:'المرتجع غير موجود'},404)}return json(await dashboard(env))}if(method==='POST'){const b=await body(request),orderCode=codeOf(b.code||b.order_code);if(!orderCode)return json({error:'باركود غير صالح'},400);const o=await env.DB.prepare('SELECT id,order_code,recipient_name,phone,order_notes,amount,area,detailed_address FROM orders WHERE order_code=?').bind(orderCode).first();if(!o)return json({error:'الطلب غير موجود'},404);const ex=await env.DB.prepare('SELECT id FROM return_events WHERE order_id=?').bind(o.id).first();if(ex)return json({ok:true,already:true,return_event:await detail(env,orderCode),dashboard:await dashboard(env)});const ins=await env.DB.prepare(`INSERT INTO return_events(order_id,return_type,reason,notes,created_by) VALUES(?,'full','مسح باركود مباشر',?,?)`).bind(o.id,String(o.order_notes||''),u.id).run();const rid=Number(ins.meta.last_row_id);const source=String(o.order_notes||'');let items=itemsFromCurrentNotes(source);if(!items.length)items=[{name:'صنف غير معروف — يحتاج تعديل',quantity:Math.max(1,qtyFromLine(source))}];for(const it of items)await env.DB.prepare('INSERT OR REPLACE INTO return_items(return_id,item_name,quantity) VALUES(?,?,?)').bind(rid,clean(it.name).slice(0,120),Math.max(1,Number(it.quantity||1))).run();return json({ok:true,already:false,return_event:await detail(env,orderCode),dashboard:await dashboard(env)})}if(method==='PUT'){const b=await body(request),orderCode=Number(b.order_code||0);if(!orderCode)return json({error:'رقم الطلب مطلوب'},400);const r=await detail(env,orderCode);if(!r)return json({error:'المرتجع غير موجود'},404);const items=Array.isArray(b.items)?b.items.filter(x=>clean(x.name)&&Number(x.quantity)>0):[];if(!items.length)return json({error:'أضف صنفًا واحدًا على الأقل'},400);await env.DB.prepare('UPDATE return_events SET return_type=?,reason=?,notes=? WHERE id=?').bind(b.return_type==='partial'?'partial':'full',clean(b.reason||r.reason),String(b.notes??r.notes),r.id).run();await env.DB.prepare('DELETE FROM return_items WHERE return_id=?').bind(r.id).run();for(const it of items)await env.DB.prepare('INSERT INTO return_items(return_id,item_name,quantity) VALUES(?,?,?)').bind(r.id,clean(it.name).slice(0,120),Math.max(1,Number(it.quantity||1))).run();return json({ok:true,return_event:await detail(env,orderCode),dashboard:await dashboard(env)})}if(method==='DELETE'){const orderCode=Number(url.searchParams.get('order_code')||0);if(!orderCode)return json({error:'رقم الطلب مطلوب'},400);const r=await detail(env,orderCode);if(!r)return json({error:'المرتجع غير موجود'},404);await env.DB.prepare('DELETE FROM return_items WHERE return_id=?').bind(r.id).run();await env.DB.prepare('DELETE FROM return_events WHERE id=?').bind(r.id).run();return json({ok:true,dashboard:await dashboard(env)})}return json({error:'طلب غير معروف'},404)}
